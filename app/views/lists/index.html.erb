<h1 class="text-center my-4">🎬 My Movie Lists</h1>

<div class="container">
  <div class="row">
    <% @lists.each do |list| %>
      <div class="col-md-4">
        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title"><%= link_to list.name, list_path(list), class: "text-decoration-none" %></h5>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<div class="text-center my-4">
  <%= link_to "➕ Créer une nouvelle liste", new_list_path, class: "btn btn-success" %>
</div>
